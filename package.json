{
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build && node build-post.js && mkdir -p dist/imag dist/vendor && cp -r imag/* dist/imag/ && cp -r vendor/* dist/vendor/ && cp script.js dist/ && cp styles.css dist/ && cp skills-lock.json dist/"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "gsap": "^3.15.0",
    "react": "19.2.7",
    "react-dom": "19.2.7",
    "vite": "8.0.16"
  }
}
